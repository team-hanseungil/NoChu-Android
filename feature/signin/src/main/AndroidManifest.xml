<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>

        <meta-data
            android:name="spotify_client_id"
            android:value="${SPOTIFY_CLIENT_ID}" />

    </application>
</manifest>